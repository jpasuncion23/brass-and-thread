/* =========================================================================
   EDIT ME — paste your Supabase project's values here.

   Where to find them: Supabase dashboard → your project → Project
   Settings (gear icon) → API.
     SUPABASE_URL      = "Project URL"
     SUPABASE_ANON_KEY = "anon public" key

   The anon key is MEANT to be public/visible in browser code — that's
   how Supabase is designed. It only works within the limits set by the
   Row Level Security policies in supabase-schema.sql (public can browse
   products, only a logged-in admin can write). Never paste the
   "service_role" key here — that one bypasses all security and must
   never appear in a website's source code.
   ========================================================================= */

const SUPABASE_URL = "https://oyujsqzueghivhjgmukn.supabase.co";
const SUPABASE_ANON_KEY = "sb_publishable_IUxn-R2u4jvmZsGQQcJZBA_dH16JXr4";
